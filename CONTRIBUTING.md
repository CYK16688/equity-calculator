# 贡献指南

感谢你对本项目的关注。欢迎提交 bug 修复、计算逻辑改进、交互优化和文档更新。

## 开发流程

1. Fork 仓库并创建主题分支。
2. 安装依赖：`npm install`。
3. 在本地运行：`npm run dev`。
4. 对计算逻辑或布局逻辑的改动，补充或更新 `tests/` 中的测试。
5. 提交前运行 `npm test` 和 `npm run build`。
6. 提交 Pull Request，并说明改动内容、验证方式和可能影响的数据口径。

## 提交约定

- 保持改动聚焦，避免在功能改动中混入无关格式化。
- 不要提交真实企业的未公开股权资料、个人信息、密钥或本地构建产物。
- 计算逻辑应优先使用确定性、可测试的纯函数。
- 面向用户的文字保持清晰，并同步考虑数据不完整和计算失败状态。

## 报告问题

提交 Issue 时，请提供复现步骤、预期结果、实际结果，以及必要的脱敏 JSON 示例。请不要上传真实的敏感股权数据。
